version: "1"
name: telegram-bot
services:
  - name: app
    scalings:
      min: 1
      max: 1
    ports:
      - port: 8000
        protocol: http
    env:
      - name: BOT_TOKEN
        secret: true
